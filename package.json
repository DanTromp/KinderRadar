{
  "name": "kinderradar",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-check.mjs && node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
